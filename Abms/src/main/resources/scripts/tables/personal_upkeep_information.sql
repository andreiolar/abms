CREATE TABLE IF NOT EXISTS PERSONAL_UPKEEP_INFORMATION(
	aptNumber varchar(4) NOT NULL,
    spatiuComun varchar(5) NOT NULL,
    suprafataApt varchar(5) NOT NULL,
    incalzire varchar(5) NOT NULL,
    apaCaldaMenajera varchar(5) NOT NULL,
    apaReceSiCanalizare varchar(5) NOT NULL,
    numarPersoane varchar(2) NOT NULL,
    gunoi varchar(10) NOT NULL,
    curent varchar(5) NOT NULL,
    gaz varchar(5) NOT NULL,
    servicii varchar(5) NOT NULL,
    gospodaresti varchar(5) NOT NULL,
    nume varchar(50) NOT NULL,
    costTotal varchar(10) NOT NULL,
    luna varchar(15) NOT NULL
);